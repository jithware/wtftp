CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

INCLUDE_DIRECTORIES (. ${NETLINK_INCLUDE_DIR} ${IWLIB_INCLUDE_DIR})

ADD_LIBRARY (wapi STATIC network.c util.c wireless.c)

TARGET_LINK_LIBRARIES (wapi ${NETLINK_LIBRARIES} ${NETLINK_GENL_LIBRARIES} ${IWLIB_LIBRARIES} m)
